<%= form_with(model: purchase_type, local: true) do |form| %>
  <% if purchase_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(purchase_type.errors.count, "error") %> prohibited this purchase_type from being saved:</h2>

      <ul>
        <% purchase_type.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :type_name %>
    <%= form.text_field :type_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
