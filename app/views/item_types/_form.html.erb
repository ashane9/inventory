<%= form_with(model: item_type, local: true) do |item_type_form| %>
  <% if item_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item_type.errors.count, "error") %> prohibited this item_type from being saved:</h2>

      <ul>
        <% item_type.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= item_type_form.label :type_name %>
    <%= item_type_form.text_field :type_name %>
  </div>

  <div class="actions">
    <%= item_type_form.submit %>
  </div>
<% end %>
